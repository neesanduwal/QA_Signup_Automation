# Signup Automation

Automates the signup flow for https://authorized-partner.vercel.app/ — no manual steps needed.

## What you need

- Python 3.8+
- Chrome browser
- pip

## How to run

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

2. Run the script:
   ```
   python signup_automation_script.py
   ```

For headless mode (browser hidden):
```
python signup_automation_script.py --headless
```

## What it does

The script goes through all 5 signup steps: terms acceptance, account setup, agency details, professional experience, and verification. It uses random test data each run (unique email and agency name).

If something goes wrong, make sure Chrome is installed and up to date.
